* text=auto

# Production executables and unit files must keep Unix line endings even when
# contributors work from Windows. CRLF makes /usr/bin/env and POSIX sh fail.
*.sh text eol=lf
*.service text eol=lf
*.timer text eol=lf
Dockerfile text eol=lf
Caddyfile text eol=lf
